{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"TAQ Suite","text":"<p>Welcome to the internal documentation for the TAQ Suite\u2014a high-performance, unified platform for ingestion, storage, simulation, and research using Trade and Quote (TAQ) tick data.</p> <p>The suite is engineered using DuckDB, Polars, and Apache Arrow to deliver lightning-fast data processing boundaries while ensuring strong type safety and robust validation via Pydantic and dataframely.</p>"},{"location":"#suite-architecture","title":"Suite Architecture","text":"<p>The platform is explicitly separated into three decoupled modules, enforcing a strict boundary between engineering, simulation, and interactive analysis.</p> <pre><code>                                Raw TAQ    \n                              (.BIN/.IDX)  \n                                   \u2502\n\n                    \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n                    \u2502           taq-etl            \u2502\n                    \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n                    \u2502 \u2022 Decompresses .lz4 logs     \u2502\n                    \u2502 \u2022 Parses binary struct arrays\u2502\n                    \u2502 \u2022 Outputs structured grids   \u2502\n                    \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n                                   \u2502\n                                   \u25bc\n                                Database       \n                       (Parquet Partition Grid)  \n                                   \u2502\n                \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n                \u25bc                                           \u25bc\n\u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510             \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n\u2502          taq-client           \u2502             \u2502        taq-backtester         \u2502\n\u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518             \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n\u2502  \u2022 User-Facing Read SDK       \u2502             \u2502  \u2022 Stateful Simulation Engine \u2502\n\u2502  \u2022 Pydantic Query Guardrails  \u2502             \u2502  \u2022 Native dataframely Schemas \u2502\n\u2502  \u2022 Returns Polars DataFrames  \u2502             \u2502  \u2022 Point-in-Time Executions   \u2502\n\u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518             \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n               \u2502                                             \u2502\n               \u25bc                                             \u25bc\n        Jupyter Notebook                              Alpha Execution   \n         &amp; Research Dfs                                 &amp; Backtests     \n</code></pre>"},{"location":"#1-taq-etl","title":"1. <code>taq-etl</code>","text":"<ul> <li>Purpose: The data processing backbone. It handles raw binary and index decompression (<code>.BIN.lz4</code> / <code>.IDX.lz4</code>) for historical NYSE/NASDAQ tick logs, parses little-endian schemas, maps daily trade and quote flags, and writes clean, sorted, date-partitioned Apache Parquet grids.</li> <li>Who it's for: Data Engineers maintaining the storage layout.</li> </ul>"},{"location":"#2-taq-backtester","title":"2. <code>taq-backtester</code>","text":"<ul> <li>Purpose: A work in progress. This module contains a backtesting environment designed to run backtests using point-in-time TAQ data to simulate market orders.</li> <li>Who it's for: Researchers testing algorithmic strategies.</li> </ul>"},{"location":"#3-taq-client","title":"3. <code>taq-client</code>","text":"<ul> <li>Purpose: The research data-access SDK. It provides a simple, foolproof, pythonic interface (<code>TaqClient</code>) that abstracts away all underlying SQL, directory structures, and file path manipulation. It lets researchers pull massive chunks of data into memory safely using Polars LazyFrames, guarded by automatic input parsing and strict data-volume memory firewalls.</li> <li>Who it's for: Quantitative Researchers working in Jupyter Notebooks or building standalone alpha studies.</li> </ul>"},{"location":"#where-should-i-go","title":"Where should I go?","text":"<ul> <li>I am a researcher looking to load TAQ data into a notebook: \ud83d\udc49 Go to the TaqClient Getting Started Guide.</li> <li>I want to know the available columns and dtypes for trades/quotes: \ud83d\udc49 Check out the API &amp; Schema Reference.</li> <li>I want to run a simulation or look at the strategy environment: \ud83d\udc49 Read the Backtester Guide.</li> <li>I want to convert raw binary TAQ data into sectioned parquets: \ud83d\udc49 View the TAQ ETL Architecture.</li> </ul>"},{"location":"#local-development-installation","title":"Local Development Installation","text":"<p>The suite is managed via <code>uv</code> for lightning-fast, reproducible builds. To clone the repository and set up your environment:</p> <pre><code># Clone the repository\ngit clone git@github.com:rvance1/taq_suite.git\ncd taq_suite\n\n# Sync development dependencies and create virtual environment\nuv sync\n</code></pre>"},{"location":"client/api/","title":"TaqClient API Reference","text":"<p>This page provides the comprehensive API documentation for interacting with the <code>taq-client</code> module. The module leverages an underlying DuckDB engine to query daily partitioned Parquet grids and automatically attaches CRSP mapping.</p>"},{"location":"client/api/#initialization","title":"Initialization","text":"<p>The package exposes a global factory function to connect to your database instance cleanly.</p> <p>Factory function to create a TaqClient instance.</p> Source code in <code>src/taq_client/__init__.py</code> <pre><code>def connect(db_path: str | None = None) -&gt; TaqClient:\n    \"\"\"Factory function to create a TaqClient instance.\"\"\"\n    return TaqClient(db_path=db_path)\n</code></pre>"},{"location":"client/api/#core-client-interface","title":"Core Client Interface","text":"<p>The main workspace interaction occurs through the <code>TaqClient</code> class wrapper. </p>"},{"location":"client/api/#taq_client.client.TaqClient","title":"<code>taq_client.client.TaqClient</code>","text":"Source code in <code>src/taq_client/client.py</code> <pre><code>class TaqClient:\n    def __init__(self, db_path: str | None = None):\n        \"\"\"Initializes the DuckDB connection and mounts the CRSP map.\"\"\"\n        self._conn = TaqDao(db_path=db_path)\n\n    def get_trades(\n        self, \n        start_date: Union[dt.date, str], \n        end_date: Union[dt.date, str], \n        tickers: Union[str, List[str]] = [],\n        ignore_warnings: bool = False\n    ) -&gt; TradeHistoryDf:\n        \"\"\"Fetches historical trades, joined with CRSP permno.\"\"\"\n\n        query = TaqQuery(\n            start_date=start_date, \n            end_date=end_date, \n            tickers=tickers, \n            ignore_warnings=ignore_warnings\n        )\n\n        return self._conn.execute_trade_query(query)\n\n    def get_quotes(\n        self, \n        start_date: Union[dt.date, str], \n        end_date: Union[dt.date, str], \n        tickers: Union[str, List[str]] = [],\n        ignore_warnings: bool = False\n    ) -&gt; QuoteHistoryDf:\n        \"\"\"Fetches historical quotes, joined with CRSP permno.\"\"\"\n\n        query = TaqQuery(\n            start_date=start_date, \n            end_date=end_date, \n            tickers=tickers, \n            ignore_warnings=ignore_warnings\n        )\n\n        return self._conn.execute_quote_query(query)\n</code></pre>"},{"location":"client/api/#taq_client.client.TaqClient.__init__","title":"<code>__init__(db_path=None)</code>","text":"<p>Initializes the DuckDB connection and mounts the CRSP map.</p> Source code in <code>src/taq_client/client.py</code> <pre><code>def __init__(self, db_path: str | None = None):\n    \"\"\"Initializes the DuckDB connection and mounts the CRSP map.\"\"\"\n    self._conn = TaqDao(db_path=db_path)\n</code></pre>"},{"location":"client/api/#taq_client.client.TaqClient.get_quotes","title":"<code>get_quotes(start_date, end_date, tickers=[], ignore_warnings=False)</code>","text":"<p>Fetches historical quotes, joined with CRSP permno.</p> Source code in <code>src/taq_client/client.py</code> <pre><code>def get_quotes(\n    self, \n    start_date: Union[dt.date, str], \n    end_date: Union[dt.date, str], \n    tickers: Union[str, List[str]] = [],\n    ignore_warnings: bool = False\n) -&gt; QuoteHistoryDf:\n    \"\"\"Fetches historical quotes, joined with CRSP permno.\"\"\"\n\n    query = TaqQuery(\n        start_date=start_date, \n        end_date=end_date, \n        tickers=tickers, \n        ignore_warnings=ignore_warnings\n    )\n\n    return self._conn.execute_quote_query(query)\n</code></pre>"},{"location":"client/api/#taq_client.client.TaqClient.get_trades","title":"<code>get_trades(start_date, end_date, tickers=[], ignore_warnings=False)</code>","text":"<p>Fetches historical trades, joined with CRSP permno.</p> Source code in <code>src/taq_client/client.py</code> <pre><code>def get_trades(\n    self, \n    start_date: Union[dt.date, str], \n    end_date: Union[dt.date, str], \n    tickers: Union[str, List[str]] = [],\n    ignore_warnings: bool = False\n) -&gt; TradeHistoryDf:\n    \"\"\"Fetches historical trades, joined with CRSP permno.\"\"\"\n\n    query = TaqQuery(\n        start_date=start_date, \n        end_date=end_date, \n        tickers=tickers, \n        ignore_warnings=ignore_warnings\n    )\n\n    return self._conn.execute_trade_query(query)\n</code></pre>"},{"location":"client/api/#example-usage","title":"Example Usage","text":"<p>The following example demonstrates how to initialize the client and fetch historical quote data for specific assets. </p> <p>Data Volume Guardrails</p> <p>The client implements automatic firewalls to prevent out-of-memory crashes:</p> <ul> <li>Specific Tickers Filtered: The maximum safe query limit is 365 days.</li> <li>Entire Market (No Tickers Provided): The maximum safe query limit is 5 days.</li> </ul> <pre><code>import taq_client as tc\n\n# 1. Connect to your database directory\nclient = tc.connect(db_path=\"home/user/tmp/taq_data\")\n\n# 2. Fetch a month of National Best Bid/Ask quote changes for Apple and Microsoft\nquotes = client.get_quotes(\n    start_date=\"1999-01-01\", \n    end_date=\"1999-01-31\", \n    tickers=[\"AAPL\", \"MSFT\"]\n)\n\n# 3. Inspect the returned dataframely / Polars structure\nprint(quotes.head())\n</code></pre>"},{"location":"client/api/#query-configurations-guardrails","title":"Query Configurations &amp; Guardrails","text":"<p>When query methods are executed on the client, user arguments are intercepted and evaluated by a strict validation lifecycle powered by Pydantic. This step guarantees data sanitization, handles date formatting, and establishes protective safeguards to protect system memory.</p>"},{"location":"client/api/#guardrail-mechanics-limitations","title":"Guardrail Mechanics &amp; Limitations","text":"<p>The validation engine processes incoming inputs through three core phases:</p> <ol> <li>Date Coercion: Input fields (<code>start_date</code> and <code>end_date</code>) accept standard Python <code>datetime.date</code> objects or standard ISO-formatted strings (e.g., <code>\"1999-01-01\"</code>). Strings are automatically parsed into native date types.</li> <li>Ticker Normalization: Tickers can be provided as a single string (e.g., <code>\"aapl\"</code>) or a list of strings (e.g., <code>[\"aapl\", \"msft\"]</code>). The validator automatically transforms all inputs into an uppercase list to ensure downstream filtering operates deterministically.</li> <li>Data Volume Firewalls: To prevent sudden Out-of-Memory (OOM) crashes caused by accidentally scanning massive multi-file collections, the query window size is cross-validated against your filtering scope:</li> </ol> Query Scope Maximum Allowed Window Error Behavior Filtered Tickers (e.g. <code>[\"AAPL\"]</code>) 365 Days Throws <code>ValueError</code> if exceeded Entire Market (No tickers provided) 5 Days Throws <code>ValueError</code> if exceeded <p>Bypassing the Firewall</p> <p>If a large-scale structural pull is explicitly required (e.g., running an archive index migration), researchers can intentionally bypass these limits by passing <code>ignore_warnings=True</code> during method invocation.</p>"},{"location":"client/api/#technical-reference","title":"Technical Reference","text":"<p>Below is the automated documentation of the Pydantic configuration parameters compiled directly from the source module:</p>"},{"location":"client/api/#taq_client.models.taq_query.TaqQuery","title":"<code>taq_client.models.taq_query.TaqQuery</code>","text":"<p>               Bases: <code>BaseModel</code></p> Source code in <code>src/taq_client/models/taq_query.py</code> <pre><code>class TaqQuery(BaseModel):\n    model_config = ConfigDict(arbitrary_types_allowed=True)\n\n    start_date: dt.date\n    end_date: dt.date\n    tickers: Optional[Union[str, List[str]]] = Field(default=None)\n    ignore_warnings: bool = Field(default=False)\n\n    # 1. Automatically convert strings like \"2026-05-28\" into dt.date objects\n    @field_validator(\"start_date\", \"end_date\", mode=\"before\")\n    @classmethod\n    def parse_dates(cls, v):\n        if isinstance(v, str):\n            return dt.datetime.strptime(v, \"%Y-%m-%d\").date()\n        return v\n\n    # 2. Ensure all tickers are uppercase lists for clean filtering\n    @field_validator(\"tickers\", mode=\"after\")\n    @classmethod\n    def normalize_tickers(cls, v):\n        if isinstance(v, str):\n            return [v.upper()]\n        return [t.upper() for t in v]\n\n    # 3. Multi-field cross validation (The Guardrail)\n    @model_validator(mode=\"after\")\n    def enforce_data_limits(self) -&gt; \"TaqQuery\":\n        if self.end_date &lt; self.start_date:\n            raise ValueError(\"end_date cannot be before start_date.\")\n\n        days_requested = (self.end_date - self.start_date).days\n        max_safe_days = 365 if self.tickers else 5\n\n        if days_requested &gt; max_safe_days and not self.ignore_warnings:\n            target = \"specific tickers\" if self.tickers else \"the entire market\"\n            raise ValueError(\n                f\"Requested {days_requested} days for {target}. \"\n                f\"This exceeds the safety limit of {max_safe_days} days. \"\n                f\"Set `ignore_warnings=True` to bypass.\"\n            )\n        return self\n</code></pre>"},{"location":"client/api/#exceptions","title":"Exceptions","text":"<p>The custom error boundaries thrown by the package when data or configurations are missing.</p>"},{"location":"client/api/#taq_client.models.exceptions.DataMissingError","title":"<code>taq_client.models.exceptions.DataMissingError</code>","text":"<p>               Bases: <code>FileNotFoundError</code></p> <p>Raised when requested TAQ data (quotes/trades) is missing.</p> Source code in <code>src/taq_client/models/exceptions.py</code> <pre><code>class DataMissingError(FileNotFoundError):\n    \"\"\"Raised when requested TAQ data (quotes/trades) is missing.\"\"\"\n    pass\n</code></pre>"},{"location":"client/api/#taq_client.models.exceptions.CrspMappingMissingError","title":"<code>taq_client.models.exceptions.CrspMappingMissingError</code>","text":"<p>               Bases: <code>FileNotFoundError</code></p> <p>Raised when the CRSP mapping files are missing from the database infrastructure.</p> Source code in <code>src/taq_client/models/exceptions.py</code> <pre><code>class CrspMappingMissingError(FileNotFoundError):\n    \"\"\"Raised when the CRSP mapping files are missing from the database infrastructure.\"\"\"\n    pass\n</code></pre>"},{"location":"client/api/#return-type-schemas","title":"Return Type Schemas","text":"<p>Data is returned to your workspace formatted as strongly typed, native Polars DataFrames wrapped by <code>dataframely</code> validation schema layouts.</p>"},{"location":"client/api/#trade-records-schema","title":"Trade Records Schema","text":"<p>               Bases: <code>Schema</code></p> Source code in <code>src/taq_client/models/schema.py</code> <pre><code>class TradeHistorySchema(dy.Schema):\n    datetime = dy.Datetime(nullable=False)\n    permno = dy.Int64(nullable=True)\n    ticker = dy.String(nullable=False)\n    price = dy.Float(nullable=False)\n    volume = dy.Int32(nullable=False)\n</code></pre>"},{"location":"client/api/#quote-records-schema","title":"Quote Records Schema","text":"<p>               Bases: <code>Schema</code></p> Source code in <code>src/taq_client/models/schema.py</code> <pre><code>class QuoteHistorySchema(dy.Schema):\n    datetime = dy.Datetime(nullable=False)\n    permno = dy.Int64(nullable=True)\n    ticker = dy.String(nullable=False)\n    bid = dy.Float(nullable=False)\n    ask = dy.Float(nullable=False)\n    bid_size = dy.Int16(nullable=False)\n    ask_size = dy.Int16(nullable=False)\n</code></pre>"},{"location":"client/getting_started/","title":"Getting Started with TaqClient","text":"<p>This guide will walk you through setting up the data-access client and pulling your first historical tick dataset. The <code>taq-client</code> is designed to provide rapid, memory-safe data queries via a streamlined Python interface.</p>"},{"location":"client/getting_started/#prerequisites","title":"Prerequisites","text":"<p>Before initialization, ensure that your workspace environment has been properly synchronized using <code>uv</code>:</p> <pre><code># Sync dependencies and activate your virtual environment\nuv sync\nsource .venv/bin/activate  # On Windows use: .venv\\Scripts\\activate\n</code></pre>"},{"location":"client/getting_started/#setting-the-database-environment-variable","title":"Setting the Database Environment Variable","text":"<p>The client looks for a local directory or network mount containing the partitioned Parquet tables. While you can pass this path explicitly into your code, the best practice is to set it as an environment variable in your terminal session or your local <code>.env</code> file:</p> <pre><code>export TAQ_DB_PATH=\"/path/to/your/local/taq_database_root\"\n</code></pre>"},{"location":"client/getting_started/#2-minute-quickstart","title":"2-Minute Quickstart","text":"<p>Create a new Python script or open a Jupyter Notebook cell, and execute the following boilerplate sequence to retrieve trade execution data.</p> <pre><code>import taq_client as tc\nimport datetime as dt\n\n# 1. Instantiate the workspace client wrapper\n# (Leaving db_path empty forces it to read your TAQ_DB_PATH environment variable)\nclient = tc.connect()\n\n# 2. Define your historical observation parameters\nstart = dt.date(1999,1,4)\nend = dt.date(1999,1,5)\ntarget_tickers = [\"AAPL\", \"MSFT\", \"INTC\"]\n\n# 3. Pull the joined Trade executions grid\nprint(f\"Fetching TAQ trades for {target_tickers}...\")\ntrades_df = client.get_trades(\n    start_date=start,\n    end_date=end,\n    tickers=target_tickers\n)\n\n# 4. View the resulting data matrix\nprint(f\"Retrieved {len(trades_df):,} total records.\")\nprint(trades_df.head())\n</code></pre>"},{"location":"client/getting_started/#basic-concepts-to-keep-in-mind","title":"Basic Concepts to Keep in Mind","text":""},{"location":"client/getting_started/#1-data-type-normalization","title":"1. Data Type Normalization","text":"<p>You do not need to worry about formatting string inputs or cleaning case types. The client's validation engine automatically handles inputs:</p> <ul> <li>Dates: Accepts native Python <code>datetime.date</code> objects or standard ISO-strings (<code>\"YYYY-MM-DD\"</code>).</li> <li>Tickers: Single strings or lists of strings are automatically cleaned, normalized to uppercase, and stripped of whitespace.</li> </ul>"},{"location":"client/getting_started/#2-built-in-memory-protections","title":"2. Built-In Memory Protections","text":"<p>To protect shared research servers and your local system from crashing due to sudden Out-of-Memory (OOM) situations, the client establishes protective queries:</p> <ul> <li>Filtering for Specific Tickers restricts your query window to a maximum of 365 days.</li> <li>Pulling the Entire Market (omitting the <code>tickers</code> argument entirely) limits your query window to a maximum of 5 days.</li> </ul> <p>To explicitly run an unusually large historical capture that breaks these limits, pass the override flag: <pre><code># Bypasses the query firewall safely for extensive extractions\nmassive_df = client.get_trades(\n    start_date=\"1999-01-01\", \n    end_date=\"1999-12-31\", \n    ignore_warnings=True\n)\n</code></pre></p>"},{"location":"client/getting_started/#3-data-format","title":"3. Data Format","text":"<p>The objects returned by <code>get_trades()</code> and <code>get_quotes()</code> are Polars DataFrames validated against a strict <code>dataframely</code> schema layer. This means you immediately inherit lightning-fast vectorized aggregation, filtering, and analysis performance without any translation lag.</p>"},{"location":"dev/backtester/","title":"TAQ Backtester Architecture","text":"<p>The <code>taq-backtester</code> module contains a stateful, event-driven portfolio simulation engine. It ingests time-series target weights, evaluates executions against historical high-frequency TAQ quote grids, and outputs time-series realised weights and performance metrics.</p>"},{"location":"dev/backtester/#current-architecture-assumptions","title":"Current Architecture &amp; Assumptions","text":"<p>The backtesting framework is explicitly structured as a systematic batch rebalancing processor. It operates with the following internal metrics:</p> <ol> <li>Time-Series Allocations: The engine takes in a continuous time-series dataset of optimal weights (<code>WeightsHistoryDf</code>) and steps through dates to map out realized asset positions over time.</li> <li>Execution Timing: The simulation filters the target alpha weights up to a designated execution cutoff parameter (defaults to <code>09:05:00</code> AM) and executes immediately against the local day's quote stream.</li> <li>Execution Model: Orders are filled completely with no market-impact latency or partial execution:<ul> <li>Buys (<code>delta_shares &gt; 0</code>): Filled entirely at the market Ask price.</li> <li>Sells (<code>delta_shares &lt; 0</code>): Filled entirely at the market Bid price.</li> </ul> </li> </ol> <p>Known Work-in-Progress Constraints</p> <p>The engine currently assumes a simplified friction model:</p> <ul> <li>No Execution Slippage: Orders are filled instantly against the first available quote, ignoring order-book queue depth or size-based price degradation.</li> <li>No Short Selling Borrow Costs: Margin costs for negative equity or share allocations are not yet factored into cash calculations.</li> </ul>"},{"location":"dev/backtester/#data-ingestion-output-pipeline","title":"Data Ingestion &amp; Output Pipeline","text":"<p>The simulator transforms theoretical allocation weights into concrete historical performance vectors:</p> <pre><code> \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n \u2502    WeightsHistoryDf         \u2502  &lt;-- 1. Input: Daily target weights\n \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n                \u2502\n                \u25bc\n \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n \u2502         Backtester          \u2502  &lt;-- 2. Processing: Evaluates fills against \n \u2502      Simulation Engine      \u2502         Bid/Ask quotes per rebalance date\n \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n                \u2502\n        \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n        \u25bc               \u25bc\n \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510 \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n \u2502 Realized    \u2502 \u2502 AUM History \u2502  &lt;-- 3. Output: Time-series weights achieved \n \u2502 Weights     \u2502 \u2502  Over Time  \u2502         from the simulated order fills\n \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518 \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n</code></pre>"},{"location":"dev/backtester/#technical-api-reference","title":"Technical API Reference","text":"<p>Below is the automated documentation of the simulation framework compiled directly from the source code module:</p>"},{"location":"dev/backtester/#taq_backtester.engine.backtester.Backtester","title":"<code>taq_backtester.engine.backtester.Backtester</code>","text":"Source code in <code>src/taq_backtester/engine/backtester.py</code> <pre><code>class Backtester():\n    def __init__(self, config: BTConfig, taq_dao: TaqDao):\n        self.config = config\n        self.taq_dao = taq_dao\n        self.current_date = self.config.start_date\n        self.holdings: SharesDf = pl.DataFrame({\"ticker\": pl.Series([], dtype=pl.String), \"shares\": pl.Series([], dtype=pl.Int64)})\n        self.cash = self.config.initial_aum\n        self.realized_holdings: SharesHistoryDf = pl.DataFrame({\"datetime\": pl.Series([], dtype=pl.Datetime), \"ticker\": pl.Series([], dtype=pl.String), \"shares\": pl.Series([], dtype=pl.Int64)})\n        self.aum_history = {}\n\n    def _record_holdings(self) -&gt; None:\n        current_dt = dt.datetime.combine(self.current_date, dt.time(0, 0))\n        holdings_snapshot: SharesHistoryDf = SharesHistorySchema.validate(\n            self.holdings.with_columns(pl.lit(current_dt).alias(\"datetime\"))\n            .select([\"datetime\", \"ticker\", \"shares\"])\n        )\n\n        if self.realized_holdings.is_empty():\n            self.realized_holdings = holdings_snapshot\n        else:\n            self.realized_holdings = pl.concat([self.realized_holdings, holdings_snapshot]).sort([\"ticker\", \"datetime\"])\n\n    def generate_orders(self, optimal_weights: WeightsDf, quote_data: QuoteHistoryDf) -&gt; SharesDf:\n        \"\"\"Generates the delta shares to trade based on the optimal weights and current holdings.\"\"\"\n\n        prices = compute_prices(quote_data)\n\n        aum = compute_aum(\n            holdings=self.holdings,\n            prices=prices,\n            cash=self.cash\n        )\n\n        optimal_shares = compute_optimal_shares(\n            optimal_weights=optimal_weights,\n            prices=prices,\n            aum=aum\n        )\n\n        delta_shares = compute_delta_shares(\n            optimal_shares=optimal_shares,\n            current_shares=self.holdings\n        )\n\n        self.aum_history[self.current_date] = aum\n        return delta_shares\n\n    def execute_orders(self, delta_shares: SharesDf, quote_data: QuoteHistoryDf) -&gt; None:\n        \"\"\"Executes the orders by calculating the cost and updating cash and holdings.\"\"\"\n\n        order_fills_raw = (\n            quote_data.join(delta_shares, on=\"ticker\", how=\"inner\")\n            .unique(subset=[\"ticker\"], keep=\"first\", maintain_order=True)\n            .with_columns(\n                pl.when(pl.col(\"shares\").gt(0))\n                .then(pl.col(\"ask\").mul(pl.col(\"shares\")))\n                .otherwise(pl.col(\"bid\").mul(pl.col(\"shares\")))\n                .alias(\"cost\")\n            )\n        )\n\n        self.cash -= order_fills_raw.get_column(\"cost\").sum()\n\n        self.holdings = add_delta_shares(\n            current_shares=self.holdings, \n            delta_shares=delta_shares\n        )\n\n        #TODO: record this, we can use it to compute slippage and other metrics later on\n        order_fills = SharesHistorySchema.validate(order_fills_raw.select([\"datetime\", \"ticker\", \"shares\"]))\n        self._record_holdings()\n\n    def rebalance(self, optimal_weights_history: WeightsHistoryDf, execute_at: dt.time = dt.time(9, 5)) -&gt; None:\n        \"\"\"Rebalances the portfolio based on the optimal weights for the given datetime.\"\"\"\n\n        optimal_weights = (\n            optimal_weights_history\n            .with_columns([\n                pl.col(\"datetime\").dt.date().alias(\"date\"),\n                pl.col(\"datetime\").dt.time().alias(\"time\")\n            ])\n            .filter(\n                pl.col(\"date\").eq(self.current_date),\n                pl.col(\"time\") &lt;= execute_at\n            )\n            .sort(\"datetime\")\n            .unique(subset=[\"ticker\", \"date\"], keep=\"last\")\n            .drop([\"date\", \"time\"])\n        )\n        if optimal_weights.is_empty():\n            print(f\"No optimal weights for date: {self.current_date}, skipping rebalance\")\n            return\n\n        optimal_weights = WeightsSchema.validate(\n            optimal_weights.filter(\n                pl.col(\"weight\").is_not_nan() &amp; pl.col(\"weight\").is_finite()\n            )\n        )\n        quote_data = self.taq_dao.load_quote_by_date(self.current_date)\n\n        delta_shares = self.generate_orders(\n            optimal_weights=optimal_weights,\n            quote_data=quote_data\n        )\n\n        self.execute_orders(\n            delta_shares=delta_shares,\n            quote_data=quote_data\n        )\n</code></pre>"},{"location":"dev/backtester/#taq_backtester.engine.backtester.Backtester.__init__","title":"<code>__init__(config, taq_dao)</code>","text":"Source code in <code>src/taq_backtester/engine/backtester.py</code> <pre><code>def __init__(self, config: BTConfig, taq_dao: TaqDao):\n    self.config = config\n    self.taq_dao = taq_dao\n    self.current_date = self.config.start_date\n    self.holdings: SharesDf = pl.DataFrame({\"ticker\": pl.Series([], dtype=pl.String), \"shares\": pl.Series([], dtype=pl.Int64)})\n    self.cash = self.config.initial_aum\n    self.realized_holdings: SharesHistoryDf = pl.DataFrame({\"datetime\": pl.Series([], dtype=pl.Datetime), \"ticker\": pl.Series([], dtype=pl.String), \"shares\": pl.Series([], dtype=pl.Int64)})\n    self.aum_history = {}\n</code></pre>"},{"location":"dev/backtester/#taq_backtester.engine.backtester.Backtester.generate_orders","title":"<code>generate_orders(optimal_weights, quote_data)</code>","text":"<p>Generates the delta shares to trade based on the optimal weights and current holdings.</p> Source code in <code>src/taq_backtester/engine/backtester.py</code> <pre><code>def generate_orders(self, optimal_weights: WeightsDf, quote_data: QuoteHistoryDf) -&gt; SharesDf:\n    \"\"\"Generates the delta shares to trade based on the optimal weights and current holdings.\"\"\"\n\n    prices = compute_prices(quote_data)\n\n    aum = compute_aum(\n        holdings=self.holdings,\n        prices=prices,\n        cash=self.cash\n    )\n\n    optimal_shares = compute_optimal_shares(\n        optimal_weights=optimal_weights,\n        prices=prices,\n        aum=aum\n    )\n\n    delta_shares = compute_delta_shares(\n        optimal_shares=optimal_shares,\n        current_shares=self.holdings\n    )\n\n    self.aum_history[self.current_date] = aum\n    return delta_shares\n</code></pre>"},{"location":"dev/backtester/#taq_backtester.engine.backtester.Backtester.execute_orders","title":"<code>execute_orders(delta_shares, quote_data)</code>","text":"<p>Executes the orders by calculating the cost and updating cash and holdings.</p> Source code in <code>src/taq_backtester/engine/backtester.py</code> <pre><code>def execute_orders(self, delta_shares: SharesDf, quote_data: QuoteHistoryDf) -&gt; None:\n    \"\"\"Executes the orders by calculating the cost and updating cash and holdings.\"\"\"\n\n    order_fills_raw = (\n        quote_data.join(delta_shares, on=\"ticker\", how=\"inner\")\n        .unique(subset=[\"ticker\"], keep=\"first\", maintain_order=True)\n        .with_columns(\n            pl.when(pl.col(\"shares\").gt(0))\n            .then(pl.col(\"ask\").mul(pl.col(\"shares\")))\n            .otherwise(pl.col(\"bid\").mul(pl.col(\"shares\")))\n            .alias(\"cost\")\n        )\n    )\n\n    self.cash -= order_fills_raw.get_column(\"cost\").sum()\n\n    self.holdings = add_delta_shares(\n        current_shares=self.holdings, \n        delta_shares=delta_shares\n    )\n\n    #TODO: record this, we can use it to compute slippage and other metrics later on\n    order_fills = SharesHistorySchema.validate(order_fills_raw.select([\"datetime\", \"ticker\", \"shares\"]))\n    self._record_holdings()\n</code></pre>"},{"location":"dev/backtester/#taq_backtester.engine.backtester.Backtester.rebalance","title":"<code>rebalance(optimal_weights_history, execute_at=dt.time(9, 5))</code>","text":"<p>Rebalances the portfolio based on the optimal weights for the given datetime.</p> Source code in <code>src/taq_backtester/engine/backtester.py</code> <pre><code>def rebalance(self, optimal_weights_history: WeightsHistoryDf, execute_at: dt.time = dt.time(9, 5)) -&gt; None:\n    \"\"\"Rebalances the portfolio based on the optimal weights for the given datetime.\"\"\"\n\n    optimal_weights = (\n        optimal_weights_history\n        .with_columns([\n            pl.col(\"datetime\").dt.date().alias(\"date\"),\n            pl.col(\"datetime\").dt.time().alias(\"time\")\n        ])\n        .filter(\n            pl.col(\"date\").eq(self.current_date),\n            pl.col(\"time\") &lt;= execute_at\n        )\n        .sort(\"datetime\")\n        .unique(subset=[\"ticker\", \"date\"], keep=\"last\")\n        .drop([\"date\", \"time\"])\n    )\n    if optimal_weights.is_empty():\n        print(f\"No optimal weights for date: {self.current_date}, skipping rebalance\")\n        return\n\n    optimal_weights = WeightsSchema.validate(\n        optimal_weights.filter(\n            pl.col(\"weight\").is_not_nan() &amp; pl.col(\"weight\").is_finite()\n        )\n    )\n    quote_data = self.taq_dao.load_quote_by_date(self.current_date)\n\n    delta_shares = self.generate_orders(\n        optimal_weights=optimal_weights,\n        quote_data=quote_data\n    )\n\n    self.execute_orders(\n        delta_shares=delta_shares,\n        quote_data=quote_data\n    )\n</code></pre>"},{"location":"dev/backtester/#ongoing-developer-roadmap","title":"Ongoing Developer Roadmap","text":"<p>If you are expanding the backtester framework, please ensure enhancements resolve one of the active tracking tasks below:</p> <ul> <li>Slippage Analytics: Implement a custom cost model inside <code>execute_orders()</code> that tracks execution timing differences compared to market mid-prices using the recorded <code>order_fills</code> dataframe.</li> <li>Look-Ahead Safeguards: Enforce a strict programmatic check confirming that <code>quote_data</code> timestamps are strictly greater than or equal to the alpha generation <code>datetime</code> to eliminate look-ahead data leaking.</li> <li>Transaction Cost Engines: Integrate basic basis-point or fixed fee models inside the execution path logic to mimic realistic clearing frictions.</li> </ul>"},{"location":"dev/etl/","title":"TAQ ETL","text":"<p>High-performance ETL pipeline for processing TAQ (Trade and Quote) financial data from binary formats into Parquet for analysis and backtesting.</p>"},{"location":"dev/etl/#installation","title":"Installation","text":"<pre><code>pip install -e .\n</code></pre> <p>This installs the <code>taq-etl</code> command-line tool.</p>"},{"location":"dev/etl/#cli-tutorial","title":"CLI Tutorial","text":"<p>The TAQ ETL tool provides a command-line interface for processing raw TAQ data. All commands follow this pattern:</p> <pre><code>taq-etl [group] [command] [options]\n</code></pre>"},{"location":"dev/etl/#basic-usage","title":"Basic Usage","text":""},{"location":"dev/etl/#processing-a-single-day","title":"Processing a Single Day","text":"<p>To process TAQ data for a single day:</p> <pre><code>taq-etl process day -d 1993-01-04 -t CQ\n</code></pre> <p>Options: - <code>-d, --date</code> \u2014 Date in <code>YYYY-MM-DD</code> format (required) - <code>-t, --type</code> \u2014 Data type: <code>CQ</code> (quotes) or <code>CT</code> (trades) (required)</p> <p>Example: <pre><code># Process trades for January 4, 1993\ntaq-etl process day -d 1993-01-04 -t CT\n\n# Process quotes for January 4, 1993\ntaq-etl process day -d 1993-01-04 -t CQ\n</code></pre></p>"},{"location":"dev/etl/#processing-a-date-range","title":"Processing a Date Range","text":"<p>To process multiple days in parallel:</p> <pre><code>taq-etl process range -s 1993-01-04 -e 1993-01-29 -t CQ\n</code></pre> <p>Options: - <code>-s, --start</code> \u2014 Start date in <code>YYYY-MM-DD</code> format (required) - <code>-e, --end</code> \u2014 End date in <code>YYYY-MM-DD</code> format (required) - <code>-t, --type</code> \u2014 Data type: <code>CQ</code> (quotes) or <code>CT</code> (trades) (required)</p> <p>Example: <pre><code># Process all trades for January 1993\ntaq-etl process range -s 1993-01-01 -e 1993-01-31 -t CT\n\n# Process all quotes for January through March 1993\ntaq-etl process range -s 1993-01-01 -e 1993-03-31 -t CQ\n</code></pre></p>"},{"location":"dev/etl/#utility-commands","title":"Utility Commands","text":"<p>All <code>utils</code> commands share a consistent interface using standard TAQ type codes:</p> <p>Standard Options: - <code>-d, --date</code> \u2014 Date in <code>YYYY-MM-DD</code> format (required) - <code>-t, --type</code> \u2014 Data type: <code>CT</code> (trades) or <code>CQ</code> (quotes) (required)</p>"},{"location":"dev/etl/#inspecting-record-sizes","title":"Inspecting Record Sizes","text":"<pre><code>taq-etl utils print-size -d 1993-01-04 -t CT\n</code></pre> <p>Prints the byte size of each record in the daily binary file.</p> <pre><code># Inspect trade record size for January 4, 1993\ntaq-etl utils print-size -d 1993-01-04 -t CT\n\n# Inspect quote record size for January 4, 1993\ntaq-etl utils print-size -d 1993-01-04 -t CQ\n</code></pre>"},{"location":"dev/etl/#hexdump-a-binary-file","title":"Hexdump a Binary File","text":"<pre><code>taq-etl utils print-bin-hexdump -d 1993-01-04 -t CT\n</code></pre> <p>Prints a formatted hexdump of the raw <code>.BIN</code> data file for debugging binary parsing.</p> <pre><code>taq-etl utils print-bin-hexdump -d 1993-01-04 -t CQ\n</code></pre>"},{"location":"dev/etl/#hexdump-an-index-file","title":"Hexdump an Index File","text":"<pre><code>taq-etl utils print-index-hexdump -d 1993-01-04 -t CT\n</code></pre> <p>Prints a formatted hexdump of the <code>.IDX</code> index file, useful for understanding the index structure.</p> <pre><code>taq-etl utils print-index-hexdump -d 1993-01-04 -t CQ\n</code></pre>"},{"location":"dev/etl/#print-index-dataframe","title":"Print Index DataFrame","text":"<pre><code>taq-etl utils print-index-df -d 1993-01-04 -t CT\n</code></pre> <p>Parses the <code>.IDX</code> index file into a DataFrame and prints it, showing ticker, date, time, byte offset, and record count columns.</p> <pre><code>taq-etl utils print-index-df -d 1993-01-04 -t CQ\n</code></pre>"},{"location":"dev/etl/#print-index-grouped-by-date","title":"Print Index Grouped by Date","text":"<pre><code>taq-etl utils print-index-by-date -d 1993-01-04 -t CT\n</code></pre> <p>Groups the parsed index by date and prints a per-date summary including ticker count, record range, and total records.</p> <pre><code>taq-etl utils print-index-by-date -d 1993-01-04 -t CQ\n</code></pre>"},{"location":"dev/etl/#data-output","title":"Data Output","text":"<p>Processed data is stored in Parquet format in the configured database path, organized by: - Data type (trades or quotes) - Year - Month - Day</p> <p>This structure allows efficient querying and analysis of historical market data.</p>"},{"location":"dev/etl/#environment-configuration","title":"Environment Configuration","text":"<p>The tool reads configuration from a <code>.env</code> file in the project root:</p> <pre><code>DATABASE_PATH=/path/to/database\n</code></pre>"}]}